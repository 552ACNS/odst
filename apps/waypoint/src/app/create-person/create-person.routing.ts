import { Routes } from '@angular/router';
import { CreatePersonComponent } from './create-person.component';

export const createPersonRoutes: Routes = [
  { path: '', component: CreatePersonComponent },
];
