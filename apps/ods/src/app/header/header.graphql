# TODO [ODST-173] the proper way to do this is with ID tokens https://auth0.com/docs/secure/tokens/id-tokens/id-token-structure
query CurrentUser {
  me {
    ...AuthenticatedUser
  }
}

fragment AuthenticatedUser on User {
  firstName
  lastName
  grade
  role
  orgs {
    name
  }
}
