import { Routes } from '@angular/router';
import { RequestAccountComponent } from './request-account.component';
export const RequestAccountRoutes: Routes = [
  { path: '', component: RequestAccountComponent },
];
