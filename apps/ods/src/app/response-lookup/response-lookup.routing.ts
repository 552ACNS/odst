import { Routes } from '@angular/router';
import { ResponseLookupComponent } from './response-lookup.component';

export const ResponseLookupRoutes: Routes = [
  { path: '', component: ResponseLookupComponent },
];
