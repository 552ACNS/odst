import { FeedbackResponseInterceptor } from './feedback-response.interceptor';

describe('FeedbackResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new FeedbackResponseInterceptor()).toBeDefined();
  });
});
