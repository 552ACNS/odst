name: docker-ods

on:
  push:
    branches:
      - 'master'
      - 'dev'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          registry: ghcr.io
          push: true
          tags: 552acns/ods:latest
